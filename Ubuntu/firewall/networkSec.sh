#use Vim and add this to the file below: install dccp /bin/true 
# /etc/modprobe.d/dccp.conf

#use Vim and add this to the file below: install sctp /bin/true 
# /etc/modprobe.d/sctp.conf

#use Vim and add this to the file below: install rds /bin/true 
# /etc/modprobe.d/rds.conf

#use Vim and add this to the file below: install tipc /bin/true 
# /etc/modprobe.d/tipc.conf